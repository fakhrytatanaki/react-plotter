import React from 'react'
import {SpaceVector,mapCoordinatesToView} from './Plotter'

//import './Axes.css'

interface Props {
    width : number,
    height : number,
    scale : SpaceVector,
    origin : SpaceVector,
}

const drawAxes = (props : Props)=>{





}



const Axes = (props : Props) => (
    <>
    </>
)



export default Axes
